name: Publish to npm

on:
  # TODO do not publish to npm for every pull request. This is only to test.
  pull_request:
    branches: [ master ]
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
          # registry-url: https://sintef.npmjs.org/
      - run: npm ci
        working-directory: projects/wonka-ui
      - run: npm publish --access public
        working-directory: projects/wonka-ui
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}
